jest.dontMock('services/chan');

describe('chan', () => {
  var chan = require('services/chan');

  describe('#find', () => {
    xit('test', () => {});
  });

  describe('#create', () => {
    xit('test', () => {});
  });

  describe('#remove', () => {
    xit('test', () => {});
  });

  describe('#getOpenChannel', () => {
    xit('test', () => {});
  });
});